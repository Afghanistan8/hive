networks:
  default: localnet

  localnet:
    url: "http://127.0.0.1:4000/api"

  studionet: {}

  # GenLayer Studio Next (Consensus v0.6 preview), chain 61997 — where HIVE is deployed.
  studio_devnet:
    url: "https://studio-next.genlayer.com/api"

paths:
  contracts: "contracts"

environment: .env
